# Local task/notes files
TODO-test-after-publish.md

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static
.vercel
.chatscreen-docs
developer-api-demo
temp
playground
docs
myoperator-plugins
COMPONENT-POLISH-TRACKER.md
onboarding

# CLI registry outputs (generated by packages/cli/scripts/generate-registry.js).
# Ignored so merges never conflict; run via root `npm install` (postinstall) or:
#   cd packages/cli && npm run generate-registry
packages/cli/src/utils/registry.ts
packages/cli/src/utils/registry-*.ts

# CLI E2E test fixture (created by `cd packages/cli && npm run test:e2e`)
tests/onboard
.claude/scheduled_tasks.lock
